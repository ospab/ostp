# Протокол тестирования

Чек-лист, который проходится перед выпуском беты и стабильной версии. Он покрывает сервер, веб-панель, десктопный GUI, Android-клиент и CLI-клиент.

Как пользоваться:

- У каждого случая есть номер, шаги и ожидаемый результат. Случай пройден, только если результат совпал полностью.
- **P1** — обязательно для беты и стабильной версии. **P2** — обязательно для стабильной версии, для беты по возможности.
- Если что-то не совпало, заведите issue с номером случая, версией, платформой и логом. Отчёт о прогоне заполняется по шаблону в конце документа.
- Сертификаты сначала проверяются на staging Let's Encrypt (`--staging`), и только потом на боевом CA: у боевого жёсткие лимиты на неудачные выпуски.

## 0. Автоматические проверки

Их выполняет CI или разработчик до ручного прогона.

| № | Проверка | Ожидается | Приоритет |
|---|---|---|---|
| A-01 | `cargo test --workspace --lib` | все тесты зелёные | P1 |
| A-02 | `cargo test -p ostp --bin ostp` (шаблоны nginx/apache/caddy, CLI) | зелёные | P1 |
| A-03 | `cargo test -p ostp-jni` (у каждой `external fun` в Kotlin есть экспорт JNI) | зелёный | P1 |
| A-04 | `flutter analyze lib` | нет `error` | P1 |
| A-05 | `node --check ostp-gui/src/main.js` | без ошибок | P1 |
| A-06 | `cargo tree -i aws-lc-sys` в корне и в `ostp-gui/src-tauri` | пусто | P1 |
| A-07 | Сборка релиза на GHA | все задачи зелёные, в релизе 21 файл | P1 |

## 1. Стенд

- **Сервер:** VPS на Debian 12 или Ubuntu 24.04 с публичным IPv4 (желательно и IPv6). Домен с A-записью (и AAAA) на этот сервер. Порты 80, 443 и 50000 (TCP+UDP) открыты.
- **Веб-серверы для раздела 2.4:** отдельные чистые машины или снапшоты с nginx, apache (Debian и RHEL-семейство) и caddy.
- **Клиенты:** Windows 10/11 x64 (arm64, если есть), Android 8+ на arm64 и armv7, Linux x64 для CLI.
- **Сети:** домашний провайдер, мобильная сеть, по возможности IPv6-only сеть с NAT64 и сеть с заметной DPI-фильтрацией.
- Версия каждого компонента записывается в отчёт (`ostp --version`, «О программе» в GUI, версия APK).

## 2. Сервер

### 2.1 Установка, обновление, конфигурация

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| S-01 | Чистая установка скриптом `install.sh`, затем `ostp setup` в режиме Server | служба `ostp` активна, `ostp links` печатает рабочие ссылки | P1 |
| S-02 | `ostp update --branch beta` на сервере с предыдущей версией | версия новая, служба перезапущена, ключи и настройки на месте | P1 |
| S-03 | `ostp migrate --dry-run` на конфиге 0.4.5 и старше (модульный v0, плоский v1) | печатаются шаги, изменения и неизвестные ключи с подсказками, файл не меняется | P1 |
| S-04 | `ostp migrate` на тех же конфигах | `config_version: 2`, `access_keys` и `api.token` на месте, сервер стартует | P1 |
| S-05 | Конфиг новее поддерживаемого (`config_version: 99`) | migrate отказывается и не трогает файл | P2 |
| S-06 | `ostp check` на корректном и сломанном конфиге | «Config OK» / понятная ошибка с указанием поля | P1 |
| S-07 | Добавить ключ в `config.json` на работающем сервере | через ~5 с новый ключ подключается без перезапуска | P1 |
| S-08 | `ostp links` и `ostp links qr` | для каждого клиента ссылки TLS/UDP и SUB (если подписки включены); в режиме qr один QR на экране: ← → переключают SUB / TLS / UDP, ↑ ↓ или Enter — клиентов, `q` — выход; при выводе в файл (`> qr.txt`) печатаются все клиенты | P1 |
| S-09 | Отсканировать QR из `ostp links qr` телефоном | приложение импортирует профиль или подписку | P1 |

### 2.2 Транспорты

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| S-10 | Клиент по UDP-ссылке | подключение, трафик идёт, RTT показывается | P1 |
| S-11 | Клиент по UoT (TCP) на 50000 | то же | P1 |
| S-12 | UoT с junk-пакетами и TCP-фрагментацией | подключение успешно | P2 |
| S-13 | Relay: `ostp init relay`, клиент ходит через relay | трафик проходит, на relay нет ключей | P2 |
| S-14 | Клиент с неверным ключом | подключения нет, сервер не падает, в логе нет ключа открытым текстом | P1 |

### 2.3 Домен и TLS со встроенным фронтендом

На машине нет веб-сервера, OSTP сам слушает 443 и 80.

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| S-20 | `ostp cert issue --staging`, домен, email, согласие с ToS | сертификат выпущен (staging), конфиг сохранён с бэкапом, предложен перезапуск | P1 |
| S-21 | `ostp cert status` | домен, издатель, срок, источник сертификата | P1 |
| S-22 | Повторный выпуск на боевом CA | доверенный сертификат, браузер открывает `https://домен/` без предупреждения (ответ 404) | P1 |
| S-23 | `http://домен/что-угодно` | 301 на https | P2 |
| S-24 | Клиент по TLS-ссылке (`type=uot&tls=1`) | подключение, трафик идёт | P1 |
| S-25 | Клиент с неверным SNI | понятная ошибка проверки сертификата | P1 |
| S-26 | Клиент со staging-сертификатом без «не проверять» / с ним | ошибка проверки / подключение | P2 |
| S-27 | `renew_days_before: 89`, подождать цикл продления | сертификат обновлён на лету, активные сессии не рвутся, перезапуск не нужен | P2 |
| S-28 | `https://домен/<webpath>/` | открывается панель (раздел 2.7) | P1 |

### 2.4 Домен и TLS через веб-сервер

Повторить для nginx, apache (Debian и RHEL) и caddy.

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| S-30 | `ostp cert issue` на машине с веб-сервером | веб-сервер найден, после подтверждения добавлен сайт (на Debian/Ubuntu — `sites-available/ostp-<домен>.conf` и симлинк `sites-enabled/ostp-<домен>.conf`, иначе `conf.d/`), конфиг прошёл проверку (`nginx -t` / `configtest` / `caddy validate`), reload выполнен; `ostp cert status` показывает все проверки зелёными | P1 (nginx), P2 (остальные) |
| S-31 | Клиент по TLS-ссылке с `path=` | подключение через 443 | P1 (nginx) |
| S-32 | Существующие сайты на этом веб-сервере | работают как раньше | P1 |
| S-33 | Заранее сломать конфиг веб-сервера и запустить установку | установка откатывается, веб-сервер продолжает работать со старым конфигом | P2 |
| S-34 | `ostp uninstall` | vhost удалён, веб-сервер перезагружен, чужие сайты не тронуты | P2 |
| S-35 | Продление сертификата | после продления выполняется `reload_command`, клиент видит новый сертификат | P2 |

### 2.5 Подписки

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| S-40 | После `ostp cert issue`: `ostp sub status`; затем `ostp sub enable` (за nginx — с `--vhost`), перезапуск, `ostp sub urls` | после выпуска сертификата подписки выключены; после enable включены, у каждого ключа есть `https://домен/sub/<токен>`, `ostp sub status` без красных пунктов | P1 |
| S-41 | `curl https://домен/sub/<токен>` | 200, по строке на ссылку (TLS первой, затем UDP), заголовки `Profile-Update-Interval`, `Profile-Title`, `Subscription-Userinfo`, `Cache-Control: no-store` | P1 |
| S-42 | `curl -H 'Accept: application/json' …` или `?format=json` | JSON с `name`, `update_interval_hours`, `links`, `usage` | P1 |
| S-43 | Несуществующий токен; токен удалённого ключа | такой же 404, как на любой другой путь | P1 |
| S-44 | `curl http://IP:50000/sub/<токен>` снаружи (открытый HTTP) | подписка не отдаётся, ответ как у заглушки | P1 |
| S-45 | То же через nginx/apache/caddy | подписка отдаётся | P1 (nginx) |
| S-46 | У ключа задан лимит, клиент потратил трафик | `usage` и `Subscription-Userinfo` показывают расход и лимит | P2 |
| S-47 | `include: ["tls"]`, `update_interval_hours: 1`, `path: "/s"` | только TLS-ссылка, интервал 1, путь `/s/<токен>` | P2 |
| S-48 | `subscription.enabled` без TLS или с путём, пересекающимся с `ws_path` / панелью | `ostp check` отклоняет конфиг с понятной ошибкой | P2 |

### 2.6 Устойчивость к зондированию

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| S-50 | `head -c 1000 /dev/urandom \| nc IP 50000` | сервер не падает, ответ не выдаёт OSTP | P1 |
| S-51 | `curl http://IP:50000/` и `curl http://IP:50000/<ws_path>` без Upgrade | 404 (или ваш fallback-сайт) | P1 |
| S-52 | Медленный заголовок (slowloris) на 50000 и 443 | соединение закрывается через ~10 с | P2 |
| S-53 | Сотни соединений с одного IP | срабатывает лимит, сервер работает для остальных | P2 |
| S-54 | TLS с чужим SNI на встроенный 443 | рукопожатие проходит с сертификатом домена, дальше 404 | P2 |

### 2.7 Веб-панель

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| S-60 | Открыть `http://127.0.0.1:9090/<webpath>/` (или через SSH-туннель) | форма входа | P1 |
| S-61 | Неверный пароль / верный | ошибка / панель | P1 |
| S-62 | Обзор при подключённом клиенте с нагрузкой | онлайн > 0, общий трафик растёт, скорость и график соответствуют нагрузке, аптайм и версия верные | P1 |
| S-63 | Блок «Как подключаются клиенты» | адрес, UDP-порт, TLS (встроенный или через веб-сервер), подписки вкл/выкл, DNS сервера совпадают с конфигом | P1 |
| S-64 | Добавить пользователя с именем и лимитом | появился в таблице, записан в `config.json`, сразу открылось окно «Поделиться» | P1 |
| S-65 | «Поделиться»: вкладки Подписка / TLS / UDP | QR виден и сканируется приложением, «Копировать» копирует ссылку | P1 |
| S-66 | Изменить имя и лимит, сбросить трафик, удалить | изменения в таблице и в `config.json`, удалённый ключ больше не подключается | P1 |
| S-67 | Клиент выбрал лимит | клиент отключён, полоса лимита красная | P2 |
| S-68 | Поиск, фильтры «Онлайн» и «У лимита», сортировка по колонкам | работают | P2 |
| S-69 | «Создать пачкой» на 10 ключей | 10 ключей в таблице и в конфиге | P2 |
| S-70 | Маршрутизация: изменить правила, сохранить | применяется сразу, записано в конфиг; некорректный JSON отклоняется | P2 |
| S-71 | Конфигурация: изменить и сохранить | файл записан, показано предупреждение о перезапуске; некорректный JSON не сохраняется | P2 |
| S-72 | Журнал | каждое действие из панели записано, очистка работает | P2 |
| S-73 | Переключить язык и тему, открыть с телефона | всё переведено, светлая тема читается, вёрстка на узком экране не ломается | P2 |

## 3. Десктопный GUI (Windows)

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| G-01 | Установка NSIS; обновление поверх предыдущей версии | запускается, профили и настройки сохранились | P1 |
| G-02 | Импорт `ostp://` через «Ссылка или подписка» и из буфера | открылся редактор с заполненными полями, включая TLS | P1 |
| G-03 | Ручное создание, изменение и удаление профиля | работает, активный профиль корректен | P1 |
| G-04 | «Поделиться» у профиля | виден QR (не пустое окно), он сканируется телефоном, ссылка копируется | P1 |
| G-05 | Импорт `https://…/sub/…` | появилась карточка подписки (имя, расход, «обновлено только что»), профили подписки помечены «sub», первый выбран | P1 |
| G-06 | На сервере сменить порт или путь, в GUI нажать «Обновить» | профили подписки обновились, выбранный остался выбранным (тот же канал), настройки фрагментации сохранились | P1 |
| G-07 | Подписка с `update_interval_hours: 1`, перезапустить GUI через час | обновилась сама | P2 |
| G-08 | Неверный URL, `http://…`, удалённый ключ | понятная ошибка на карточке и в уведомлении; неработающий URL не сохраняется | P1 |
| G-09 | QR подписки на карточке | сканируется телефоном, импортируется как подписка | P2 |
| G-10 | Удалить подписку | подписка и её профили удалены, ручные профили на месте | P1 |
| G-11 | Подключение UDP, UoT, TLS (встроенный и через nginx) | подключается, трафик идёт, скорость и RTT видны | P1 |
| G-12 | TUN-режим (с wintun.dll и без) | с драйвером работает, без него показывает инструкцию | P1 |
| G-13 | Kill switch, исключения доменов/IP/процессов, MUX, MTU | работает; изменение при подключении применяется горячей перезагрузкой | P2 |
| G-14 | «Проверка сети» → «Проверить сервер» на UDP-профиле | таблица адрес × канал с RTT, итоговая строка | P1 |
| G-15 | То же на TLS-профиле | проверяется только TLS на порту профиля | P1 |
| G-16 | «Проверка пути (TTL)» после проверки сервера | ряд TTL с отмеченными ответами и вывод | P2 |
| G-17 | «Тест DPI сети» | список проверок и итог, ~10 с | P1 |
| G-18 | Проверки при подключённом VPN | выполняются, туннель не рвётся | P2 |
| G-19 | Автозапуск, автоподключение, трей, темы | работают | P2 |

## 4. Android

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| M-01 | Установка APK (arm64 и armv7); обновление поверх предыдущей | запускается, профили сохранились | P1 |
| M-02 | Импорт `ostp://` по QR, ссылкой и вручную | профиль создан, TLS-поля перенесены | P1 |
| M-03 | Импорт подписки по QR и ссылкой | карточка в «Подписках» (расход, полоса лимита, «обновлено»), профили помечены «subscription» | P1 |
| M-04 | Обновить подписку после изменения на сервере; удалить подписку | как G-06 и G-10 | P1 |
| M-05 | Подписка с истёкшим интервалом, запустить приложение | обновилась в фоне при запуске | P2 |
| M-06 | Ошибки подписки (404, http, нет сети) | текст ошибки на карточке, приложение не падает | P1 |
| M-07 | Подключение UDP, UoT, TLS | работает | P1 |
| M-08 | Раздельное туннелирование по приложениям | исключённые приложения идут мимо VPN | P2 |
| M-09 | Смена сети Wi-Fi ↔ мобильная при подключении | переподключение без ручных действий | P1 |
| M-10 | Пробер: проверка сервера | результаты без ошибки «No implementation found» | P1 |
| M-11 | Пробер: TTL и DPI-батарея, в том числе при подключённом VPN | результаты, приложение не зависает | P1 |
| M-12 | «Поделиться» профилем | QR и ссылка | P2 |

## 5. CLI-клиент

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| C-01 | `ostp connect ostp://…` (UDP, UoT, TLS) | подключение, SOCKS5 на `127.0.0.1:1088` работает | P1 |
| C-02 | `ostp import ostp://…` | конфиг клиента записан | P1 |
| C-03 | `ostp import https://…/sub/…` | показаны имя подписки, расход, список ссылок, выбор номера; выбранная ссылка записана в конфиг | P1 |
| C-04 | `ostp import http://…`, неверный токен | понятная ошибка, конфиг не тронут | P1 |
| C-05 | TUN на Linux, `eval $(ostp proxy-env)` | работает | P2 |

## 6. Совместимость

| № | Шаги | Ожидается | Пр. |
|---|---|---|---|
| X-01 | Клиенты предыдущей стабильной версии (GUI, Android, CLI) против нового сервера по UDP и UoT | подключаются | P1 |
| X-02 | Новые клиенты против сервера предыдущей стабильной версии по UDP и UoT | подключаются | P1 |
| X-03 | Ссылки, выданные предыдущими версиями (`type=tcp`, без `type`, IPv6 в скобках) | импортируются во всех клиентах одинаково | P1 |
| X-04 | Конфиги сервера и клиента от предыдущих версий без правок | стартуют; `ostp` подсказывает про `migrate` | P1 |

## 7. Критерии выпуска

- **Бета:** все случаи раздела 0 и все P1 пройдены. Непройденные P2 перечислены в заметках к релизу как известные проблемы.
- **Стабильная версия:** пройдены все случаи, включая P2, на всех платформах из раздела 1.
- Любой краш, потеря профилей или ключей, утечка ключа в лог или трафик мимо туннеля при включённом kill switch — блокер независимо от приоритета.

## Шаблон отчёта

```
Версия: v0.4.6-beta.N        Дата:            Тестировщик:
Сервер: ОС, IP-версии, фронтенд (builtin / nginx / apache / caddy)
Клиенты: Windows …, Android … (модель, ABI), Linux …
Сети: …

Пройдено: S-01, S-02, …
Не пройдено: S-27 — <что произошло>, лог: <ссылка>, issue: #…
Не проверялось: G-07 — <почему>
Вывод: выпускать / не выпускать
```
