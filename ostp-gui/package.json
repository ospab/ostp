{
  "name": "ostp-gui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "cargo build -p ostp-tun-helper && tauri dev",
    "build": "cargo build -p ostp-tun-helper --release && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
